import 'package:flutter/material.dart';

class ChangeBudgetScreen extends StatelessWidget {
  const ChangeBudgetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}